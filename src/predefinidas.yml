respostas:
  quantos_pacientes:
    query: "select count(distinct) as total pacientes from atendimentos"
    templates: "total de pacientes unicos: {}"
  quantos_prestadores:
    query: "select count(distinct) as qtde from prestadores"
    templates: "total de prestadores unicos: {}"
